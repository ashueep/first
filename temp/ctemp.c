#include<stdio.h>

#define ll 					long long int
#define gof 				for(long long int i = 0 ; i < n ; i++)
#define for1(i,s,e) 		for(long long int i = s ; i < e ; i++)
#define forr(i,e,s) 		for(long long int i = e ; i >= s; i--)
#define mkarr(T,arr,size)	T* arr = new int [size]
#define gthru(i,n)			for(long long int i = 0 ; i < n ; ++i)
#define INIT0(arr)			memset(arr, 0, n*sizeof(arr[0]));

const ll ARR = 1e5;
const ll MOD = 1e9+7;
const ll INF = 1e18;

void solve(){

	

}

int main(){
	int t = 1;
	scanf("%d",&t);
	while(t--) solve();
	return 0;
}

